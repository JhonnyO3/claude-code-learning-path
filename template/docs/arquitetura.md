# Arquitetura

Documento importado pelo CLAUDE.md. Descreva aqui os contornos do sistema:

- Camadas: core (regras puras) <- api <- web.
- Dependências entre módulos (o que pode importar o quê).
- Decisões arquiteturais (ADendas/ADRs), padrões adotados.
- Pontos de integração externos (DB, filas, serviços, MCPs).

Mantenha curto e atualizado. Detalhes muito longos viram ADRs em docs/adr/.
